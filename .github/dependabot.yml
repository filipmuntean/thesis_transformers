version: 1
updates:
  # Define your update schedule here (e.g., daily, weekly, etc.).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
open-pull-requests-limit: 10   # maximum number of open pull requests at a time
    create:
      - security